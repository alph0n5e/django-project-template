from .base import *

# Database
# https://docs.djangoproject.com/en/{{ docs_version }}/ref/settings/#databases

ALLOWED_HOSTS += [
    # usually your domain name, or IP address from where the app is served
]
